{
  "name": "3altayer-akhil-enterprise",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=admin-dashboard",
    "build": "npm run build --workspace=admin-dashboard",
    "start": "npm run start --workspace=admin-dashboard"
  },
  "workspaces": [
    "admin-dashboard"
  ]
}